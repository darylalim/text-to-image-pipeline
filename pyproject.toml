[project]
name = "text-to-image-pipeline"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "accelerate",
    "diffusers",
    "gradio",
    "python-dotenv",
    "sentencepiece",
    "torch",
    "transformers",
]

[dependency-groups]
dev = [
    "ruff",
    "ty",
    "pytest",
]
